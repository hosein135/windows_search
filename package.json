{
  "name": "windows-search",
  "version": "0.1.0",
  "private": true,
  "description": "Demo Windows GUI search app: Node.js + Electron GUI, MongoDB storage, GPU-accelerated ranking via WebGPU across every GPU process (CPU worker-pool fallback), all-core chunked parallel import",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "start:gpu-unsafe": "electron . --gpu-unsafe",
    "start:no-helpers": "electron . --no-gpu-helpers",
    "import": "node scripts/import-cli.js",
    "import:parallel": "node scripts/import-cli.js --parallel",
    "import:bun": "bun scripts/import-cli.js --parallel",
    "make-sample": "node scripts/make-sample-data.js",
    "smoke": "node scripts/smoke-test.js",
    "hardware": "node scripts/hardware-cli.js"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "mongodb": "^6.10.0"
  },
  "devDependencies": {
    "electron": "^33.2.0"
  }
}
